{

}

// Set to always Copy to Output Dierectory - copies it to the location where the binary lives (the output of the compiled program)
// so when the application is executing, it can access the appsettings.json file.